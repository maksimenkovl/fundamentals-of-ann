# Окружение дисциплины «Основы искусственных нейронных сетей»
# Python 3.10+. Установка: pip install -r requirements.txt
# Все практические работы выполнимы на CPU; GPU ускоряет ПР 10 и Кейс 2
# (подробнее — docs/reproducibility.md).

numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4
scikit-learn>=1.4,<2
torch>=2.2,<3
torchvision>=0.17,<1
pillow>=10.0,<12

# Работа в ноутбуках
jupyterlab>=4.0,<5

# Загрузка данных и проверки
requests>=2.31,<3
pytest>=8.0,<9
