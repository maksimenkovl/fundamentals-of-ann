# КИМ-07. Практическая работа 7 «Регуляризация (борьба с переобучением)»

## 1. Назначение и формируемые результаты

КИМ формирует навык применения методов регуляризации (Model Checkpointing, Early Stopping, Weight Decay, Dropout) с использованием PyTorch и понимания режимов `model.train()` / `model.eval()`.

## 2. Привязка к КРМ

| Компетенция | Индикатор | Уровень | Роль | Трудовая функция |
| --- | --- | --- | --- | --- |
| DL-1 | DL-1.1 | Б | ML Researcher | Научные исследования и эксперименты |

## 3. Цель работы

Освоить методы регуляризации для борьбы с переобучением нейронных сетей.

## 4. Условия проведения

- **Аудитория:** компьютерный класс.
- **ПО:** Python 3.10+, PyTorch, scikit-learn, matplotlib.
- **Время:** 4 ак. часа (2 пары).
- **Вариантность:** параметры датасета `make_classification` — по варианту.

## 5. Материалы и ресурсы

- Стартовый ноутбук-заготовка — [`attachments/pr07-regularization-starter.ipynb`](attachments/pr07-regularization-starter.ipynb); окружение и данные — [`requirements.txt`](../requirements.txt), [`scripts/download_data.py`](../scripts/download_data.py), [`docs/reproducibility.md`](../docs/reproducibility.md).
- PyTorch — [`resources/software/python-libs/`](../resources/software/python-libs/README.md).
- Лекция 7 «Регуляризация (борьба с переобучением)».

## 6. Задание

1. Реализовать MLP средствами PyTorch (тензоры, autograd, `nn.Module`), сравнить с собственным модулем из ПР 1–3.
2. Сгенерировать данные (`make_classification`, ≥20 признаков, часть шумовых), разделить на train/validation/test.
3. Создать условия переобучения (2–3 скрытых слоя по 256 нейронов, много эпох).
4. Реализовать Model Checkpointing.
5. Реализовать Early Stopping с `patience`.
6. Исследовать Weight Decay при коэффициентах `0; 1e-4; 1e-2; 1e-1`.
7. Исследовать Dropout при вероятностях `0; 0.2; 0.5; 0.8`.
8. Продемонстрировать разницу `model.train()` / `model.eval()`.
9. Свести результаты в таблицу.
10. **Дополнительно:** повтор на MNIST.

## 7. Формат сдачи

- Ноутбук с кодом, сводной таблицей и выводами.
- **Защита** на следующем занятии.

## 8. Критерии и шкала

[`rubric-07-regularization.md`](rubric-07-regularization.md), 10-балльная шкала.

## 9. Правила использования внешних ресурсов и генеративного ИИ

- Документация PyTorch — разрешена.
- Генеративный ИИ — для пояснений; эксперименты проводятся на данных студента (seed по варианту).
- **Анти-чит:** сводная таблица формируется по результатам собственных запусков; режимы `train/eval` должны быть явно продемонстрированы и объяснены.

## 10. Вопросы для защиты

21. Weight Decay (L2). 22. Dropout (обучение и инференс). 26. Batch Normalization (режимы). 20. Model Checkpointing и Early Stopping.
