# КИМ-08. Практическая работа 8 «Стабилизация градиентов и ускорение обучения»

## 1. Назначение и формируемые результаты

КИМ формирует понимание проблем затухания и взрыва градиентов в глубоких сетях и методов стабилизации: выбор функций активации, инициализация весов (Ксавье, Хе), Batch Normalization.

## 2. Привязка к КРМ

| Компетенция | Индикатор | Уровень | Роль | Трудовая функция |
| --- | --- | --- | --- | --- |
| DL-1 | DL-1.1 | Б | ML Researcher | Научные исследования и эксперименты |

## 3. Цель работы

Исследовать методы стабилизации градиентов и ускорения обучения глубоких нейронных сетей.

## 4. Условия проведения

- **Аудитория:** компьютерный класс.
- **ПО:** Python 3.10+, PyTorch, matplotlib.
- **Время:** 4 ак. часа (2 пары).
- **Вариантность:** глубина сети (6–8 слоёв) — по варианту.

## 5. Материалы и ресурсы

- Стартовый ноутбук-заготовка — [`attachments/pr08-gradient-stabilization-starter.ipynb`](attachments/pr08-gradient-stabilization-starter.ipynb); окружение и данные — [`requirements.txt`](../requirements.txt), [`scripts/download_data.py`](../scripts/download_data.py), [`docs/reproducibility.md`](../docs/reproducibility.md).
- MNIST — [`resources/datasets/`](../resources/datasets/README.md).
- PyTorch — [`resources/software/python-libs/`](../resources/software/python-libs/README.md).
- Лекция 8 «Стабилизация градиентов и ускорение обучения».

## 6. Задание

1. Реализовать глубокий MLP (6–8 скрытых слоёв) на MNIST с `sigmoid`.
2. Логировать норму градиентов по слоям, продемонстрировать затухание.
3. Заменить `sigmoid` на `tanh` и `ReLU`.
4. Исследовать инициализацию (нулевая, `N(0,1)`, Ксавье, Хе).
5. Добавить Batch Normalization.
6. Продемонстрировать режимы BN (training / EMA) через `model.train()` / `model.eval()`.
7. Сформулировать общий вывод.

## 7. Формат сдачи

- Ноутбук с логами нормы градиентов, графиками и общим выводом.
- **Защита** на следующем занятии.

## 8. Критерии и шкала

[`rubric-08-gradient-stabilization.md`](rubric-08-gradient-stabilization.md), 10-балльная шкала.

## 9. Правила использования внешних ресурсов и генеративного ИИ

- Документация PyTorch — разрешена.
- Генеративный ИИ — для пояснений; логи нормы градиентов строятся по реальным запускам.
- **Анти-чит:** исследование инициализаций и BatchNorm должно быть проведено на собственных данных; выводы объясняются на защите.

## 10. Вопросы для защиты

23. Затухание и взрыв градиентов. 24. Функции активации `sigmoid`, `tanh`, `ReLU`, `softmax` и их выбор. 25. Инициализация Ксавье и Хе. 26. Batch Normalization (режимы).
