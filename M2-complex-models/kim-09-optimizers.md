# КИМ-09. Практическая работа 9 «Алгоритмы оптимизации нейронной сети»

## 1. Назначение и формируемые результаты

КИМ формирует понимание и навык реализации алгоритмов оптимизации: Momentum SGD, RMSprop, Adam; исследование их чувствительности к скорости обучения.

## 2. Привязка к КРМ

| Компетенция | Индикатор | Уровень | Роль | Трудовая функция |
| --- | --- | --- | --- | --- |
| DL-1 | DL-1.1 | Б | ML Engineer | Оптимизация производительности и масштабирование моделей |

## 3. Цель работы

Освоить реализацию и сравнение алгоритмов оптимизации нейронных сетей.

## 4. Условия проведения

- **Аудитория:** компьютерный класс.
- **ПО:** Python 3.10+, PyTorch, matplotlib.
- **Время:** 4 ак. часа (2 пары).
- **Вариантность:** набор исследуемых `lr` — по варианту.

## 5. Материалы и ресурсы

- Стартовый ноутбук-заготовка — [`attachments/pr09-optimizers-starter.ipynb`](attachments/pr09-optimizers-starter.ipynb); окружение и данные — [`requirements.txt`](../requirements.txt), [`scripts/download_data.py`](../scripts/download_data.py), [`docs/reproducibility.md`](../docs/reproducibility.md).
- MNIST — [`resources/datasets/`](../resources/datasets/README.md).
- PyTorch — [`resources/software/python-libs/`](../resources/software/python-libs/README.md).
- Лекция 9 «Алгоритмы оптимизации нейронной сети».

## 6. Задание

1. Реализовать Momentum SGD (в собственном модуле или явным циклом на тензорах PyTorch без `torch.optim`), сверить с `torch.optim.SGD(momentum=…)`.
2. Обучить одну модель четырьмя оптимизаторами (SGD, SGD+Momentum, RMSprop, Adam) при одинаковом `lr`, построить кривые Loss.
3. Подобрать оптимальный `lr` для каждого оптимизатора.
4. Исследовать чувствительность «`lr` → качество на validation».
5. Свести результаты в таблицу (эпохи до порога accuracy, итоговое качество, время).
6. **Дополнительно:** работа при разных размерах мини-батча.

## 7. Формат сдачи

- Ноутбук с кривыми Loss, сводной таблицей и выводами.
- **Защита** на следующем занятии.

## 8. Критерии и шкала

[`rubric-09-optimizers.md`](rubric-09-optimizers.md), 10-балльная шкала.

## 9. Правила использования внешних ресурсов и генеративного ИИ

- Документация PyTorch — разрешена.
- Генеративный ИИ — для пояснений; реализация Momentum SGD должна быть выполнена вручную и сверена с `torch.optim`.
- **Анти-чит:** кривые Loss и сводная таблица — по собственным запускам; сверка с эталоном обязательна.

## 10. Вопросы для защиты

27. Momentum SGD, RMSprop, Adam. 11. Алгоритм обратного распространения ошибки. 12. Градиентный спуск.
